name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python=3.9
  - pip

    # Package dependencies
  - numpy
  - bitarray
  - openbabel
  
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  # - pip:
  #   - openbabel-wheel
  #   - codecov

