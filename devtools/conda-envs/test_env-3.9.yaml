name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python=3.9
  - pip

    # Package dependencies
  - numpy
  - bitarray
  - openbabel
  
    # Testing
  - pytest
  - pluggy=0.13.1
  - pytest-cov
  - codecov

    # Pip-only installs
  # - pip:
  #   - openbabel-wheel
  #   - codecov

